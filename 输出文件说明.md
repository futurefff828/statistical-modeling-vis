# 输出文件说明

本文档用于说明 `outputs/` 文件夹中各类输出文件的具体意义、适用场景，以及当前可视化图片表达的研究含义。

## 一、输出目录总体结构

当前输出文件主要分为四类：

```text
outputs/
├── figures/final/       # 最终图片输出，包含PNG和PDF两个版本
├── tables/final/        # 论文可直接引用的最终结果表
├── tables/model_results/# 更完整的模型估计结果
├── tables/diagnostics/  # 数据诊断、变量说明和模型诊断结果
└── logs/                # 运行日志
```

其中，

`outputs/tables/final/` 是写论文时优先使用的表格目录；

`outputs/tables/model_results/` 是复查模型细节和重新画系数图时使用的目录；

`outputs/tables/diagnostics/` 是解释数据清洗、变量构造和模型诊断时使用的目录；

`outputs/figures/final/` 是当前已经生成的论文图；

`outputs/logs/` 用于记录代码运行过程。

## 二、最终论文表格：outputs/tables/final

### 1. paper_table_1_main_regression.csv

该表是主回归和核心稳健性结果表。它包括固定效应基线、主模型、替换解释变量等结果。

主要用途是支撑论文的核心结论：AI关注度或AI应用水平与制造业企业提质增效指数之间存在显著正相关关系。

表中字段含义如下：

- `模型`：当前结果对应的模型名称，例如固定效应基线、主模型、替换X等。
- `被解释变量`：当前模型使用的Y，例如提质增效指数。
- `核心解释变量`：当前模型关注的X，例如年报AI词频、MD&A AI词频、综合AI应用指数。
- `系数`：核心解释变量的估计系数，并带有显著性星号。
- `标准误`：公司聚类稳健标准误。
- `t值`：用于显著性判断的统计量。
- `p值`：显著性检验概率值。
- `样本量`：进入该模型估计的企业-年份观测数。
- `拟合指标`：模型拟合程度指标，一般为调整R²或其他对应模型指标。
- `说明`：该模型在论文中的解释用途。

### 2. paper_table_2_robustness.csv

该表是替代指标和样本处理稳健性结果表。它主要包括替换Y为TFP、熵权法指数、PCA指数，以及研发强度缺失处理后的结果。

主要用途是说明主结论是否依赖某一个被解释变量构造方式，或是否只存在于研发数据完整的样本中。

需要注意的是，研发缺失处理结果不显著，因此它应被解释为主结论的边界：AI关注度与提质增效之间的正相关关系在研发数据较完整的样本中更清楚，在扩大样本并处理研发缺失后明显变弱。

### 3. paper_table_3_mechanism.csv

该表是机制检验结果表。它用于解释AI关注度为什么可能与提质增效相关。

当前机制主要包括：

- AI关注度与实际AI应用指数之间的关系；
- AI关注度与资产周转率之间的关系；
- AI关注度与劳动生产率之间的关系；
- AI关注度与TFP之间的关系；
- AI应用或AI关注度与质量子指数、效率子指数之间的关系；
- AI落地转化效率与提质增效之间的探索性关系。

该表不是严格的中介效应因果检验，而是机制相关性检验。论文中应表述为“可能通过这些经营环节产生影响”。

### 4. paper_table_4_heterogeneity.csv

该表是异质性分析结果表。它比较不同企业类型中AI关注度与提质增效的关系是否不同。

当前分组包括：

- 国企与非国企；
- 东部地区与非东部地区；
- 高技术制造业与非高技术制造业；
- 高AI落地转化效率与低AI落地转化效率。

主要用途是回答“AI对哪些企业更相关”。目前结果显示，国企、东部地区和高技术制造业企业中的AI赋能关系更明显。

### 5. paper_table_5_conversion_moderation.csv

该表是AI落地转化效率调节效应检验表。它检验企业将AI文本关注转化为实际AI应用的能力，是否会强化AI关注度与提质增效之间的关系。

表中包含低转化效率组AI词频、高转化效率虚拟变量、AI词频与高转化效率交互项等结果。

当前交互项不显著，因此该表更适合作为探索性分析，而不是主结论。它说明“AI落地转化效率越高，AI赋能效果越强”这一说法目前证据不足。

### 6. paper_table_6_advanced_models.csv

该表是扩展模型结果表，包括分位数固定效应、非线性固定效应和交叉拟合部分线性模型。

它的作用不是替代主模型，而是从不同角度检验主结论：

- 分位数固定效应：观察AI作用是否在低提质增效企业中更明显。
- 非线性固定效应：检验AI关注度是否存在边际递减或倒U型关系。
- 交叉拟合部分线性模型：用机器学习方法净化控制变量、年份和行业影响后，再估计AI词频的净相关关系。

当前结果支持这样的表述：AI关注度与提质增效的正相关关系不完全依赖传统线性控制设定，但非线性证据并不明显。

### 7. paper_table_A1_boundary.csv

该表是边界检验结果表，主要展示公司-年份双向固定效应模型。

它的重要性很高，因为它说明当进一步控制企业固定效应后，主结论会明显变弱，部分系数甚至转为负值。这意味着本文不能把结果解释为严格因果效应，而应谨慎表述为“行业和年份可比条件下的稳健正相关关系”。

该表适合放在附录或研究局限部分，用于主动说明结论边界。

## 三、完整模型结果：outputs/tables/model_results

### 1. baseline_regression_results.csv

该表保存基准回归和基础稳健性模型的完整结果。相比 `paper_table_1_main_regression.csv`，它更偏向代码复核，包含模型公式、核心变量、R²、调整R²等信息。

适用场景：

- 检查主模型公式是否正确；
- 复核每个模型使用的核心变量；
- 重新生成主模型和稳健性系数图。

### 2. robustness_regression_results.csv

该表保存稳健性回归的底层结果，主要用于支撑替换X、替换Y等稳健性检验。

适用场景：

- 检查替换变量后的系数方向和显著性；
- 复查稳健性图中每个点的来源；
- 与最终论文表 `paper_table_2_robustness.csv` 对照。

### 3. final_fixed_effects_research_results.csv

该表是最终固定效应研究模型结果总表，覆盖主模型、替代X、替代Y、机制相关模型等多类结果。

适用场景：

- 作为重新整理论文回归表的基础文件；
- 作为综合系数图的数据来源；
- 检查不同模型之间样本量、年份数、企业数是否一致。

### 4. mechanism_regression_results.csv

该表保存机制检验的完整模型结果。它解释AI关注度是否与实际AI应用、资产周转率、劳动生产率、TFP、质量子指数、效率子指数等变量相关。

适用场景：

- 重画机制路径图；
- 检查每条机制路径是否显著；
- 判断哪些机制适合写入论文主文，哪些只适合作为补充结果。

### 5. heterogeneity_regression_results.csv

该表保存异质性分组回归的完整结果，包含每个分组的模型名称、分组变量、系数、标准误、p值和样本量。

适用场景：

- 重画异质性森林图；
- 对比国企/非国企、东部/非东部、高技术/非高技术等组间差异；
- 判断异质性结论是否应该放入主文。

### 6. conversion_moderation_results.csv

该表保存AI落地转化效率调节效应模型结果。

适用场景：

- 检查交互项是否显著；
- 判断转化效率是否能作为调节机制；
- 为论文中的探索性分析提供数据依据。

### 7. quantile_fe_results.csv

该表保存分位数固定效应模型结果，当前包括25%、50%和75%分位点。

适用场景：

- 分析AI作用是否在不同提质增效水平企业中存在差异；
- 支撑“低提质增效企业改善空间更大”的表述；
- 重画分位数系数变化图。

### 8. nonlinear_ai_effect_results.csv

该表保存非线性模型结果，主要包括AI词频一次项和二次项。

适用场景：

- 检验AI关注度是否存在边际递减；
- 判断是否能够写出倒U型或非线性结论；
- 当前结果中二次项不显著，因此不适合强调非线性机制。

### 9. cross_fitted_partial_linear_results.csv

该表保存交叉拟合部分线性模型结果。该模型用机器学习方法对控制变量、年份和行业影响进行净化，再估计AI词频与提质增效之间的净相关关系。

适用场景：

- 检验主结论是否依赖线性控制设定；
- 作为统计建模创新点之一；
- 与固定效应主模型系数进行对照。

### 10. firm_year_fe_boundary_tests.csv

该表保存企业固定效应与年份固定效应的边界检验结果。

适用场景：

- 判断主结论在更严格固定效应设定下是否仍然成立；
- 支撑论文局限性和谨慎解释；
- 避免将当前结果过度表述为因果效应。

## 四、诊断与数据说明表：outputs/tables/diagnostics

### 1. data_cleaning_summary.csv

该表记录主要数据集的样本规模、企业数量和年份范围。

用途是说明样本清洗后保留了多少观测，以及样本覆盖的年份区间。

### 2. sample_integrity_checks.csv

该表用于检查样本完整性，尤其是制造业筛选是否正确。

表中包括非制造业样本、非C类行业样本、特定非制造业企业样本是否错误进入建模数据。`passed` 表示该检查是否通过。

### 3. missingness_summary.csv

该表统计各变量缺失数量和缺失率。

它主要用于解释为什么不同模型样本量不同。例如，研发强度缺失会影响主模型样本；某些机制变量缺失会影响机制检验样本。

### 4. descriptive_statistics.csv

该表是主要变量的描述性统计，包括样本量、均值、标准差、最小值、四分位数、中位数和最大值。

适合放在论文的描述性统计表中，用于说明变量分布是否合理。

### 5. variable_dictionary.csv

该表是变量字典，记录变量名、变量角色和变量定义。

适合用于复现说明、附录或交给可视化人员理解字段含义。

### 6. quality_efficiency_index_components.csv

该表解释提质增效指数的组成，包括质量维度和效率维度下的具体指标及含义。

它是说明Y变量构造的重要文件，适合在论文变量构造部分引用。

### 7. candidate_xy_screening_results.csv

该表是候选X-Y组合筛选结果，记录不同解释变量和被解释变量组合下的回归结果。

用途是帮助理解为什么最终选择当前X和Y。它不是最终论文主表，但可以作为建模探索和变量选择依据。

### 8. heteroskedasticity_tests.csv

该表保存异方差检验结果，例如Breusch-Pagan检验统计量和p值。

用途是说明为什么需要使用稳健标准误。若存在异方差，普通标准误可能低估不确定性。

### 9. vif_diagnostics.csv

该表保存方差膨胀因子VIF，用于检查控制变量之间是否存在严重多重共线性。

如果某些变量VIF过高，说明变量之间高度相关，可能影响系数稳定性。当前文件可用于模型诊断说明。

## 五、运行日志：outputs/logs

### pipeline.log

该文件记录完整管线运行过程，包括数据清洗、变量构造、模型估计、表格输出和图片输出。

用途包括：

- 复查代码是否完整运行；
- 定位运行失败或警告；
- 确认每一步输出文件是否生成；
- 为可复现性提供日志证据。

该文件不适合直接放入论文，但适合项目复现检查。

## 六、当前可视化图片：outputs/figures/final

当前图片同时输出了PNG和PDF两个版本。PNG适合放入Markdown、Word或预览；PDF适合论文排版和矢量化印刷。

### 1. fig1_ai_trend.png / fig1_ai_trend.pdf

图名含义：AI关注与实际AI应用趋势。

该图展示制造业企业年报AI词频、MD&A AI词频和实际AI应用指数随年份变化的趋势。

它主要用于说明样本背景：制造业企业对AI的文本关注总体上升，实际AI应用指数也呈现变化和上升趋势。

该图不能直接证明AI会提升提质增效，因为它只是时间趋势图，可能受到宏观环境、政策变化和行业周期共同影响。论文中应把它放在描述性分析部分，而不是作为因果证据。

### 2. fig2_quality_efficiency_trend.png / fig2_quality_efficiency_trend.pdf

图名含义：提质增效指数及其子指数趋势。

该图展示提质增效指数、质量子指数和效率子指数的年度变化。

它主要用于说明Y变量不是静态指标，而是在样本期间存在阶段性变化；同时，质量子指数和效率子指数走势并不完全相同，说明将两者合成为综合提质增效指数具有必要性。

该图的作用是解释被解释变量的背景变化，不能单独说明AI与提质增效之间的相关关系。

### 3. fig3_baseline_robustness_coefficients.png / fig3_baseline_robustness_coefficients.pdf

图名含义：基准模型与核心稳健性系数图。

该图一般以系数点和置信区间展示主模型及替代X模型中核心解释变量的估计结果。横轴通常表示回归系数大小，纵轴表示不同模型或变量口径。

它回答的问题是：当使用不同AI变量或不同模型口径时，AI相关变量的系数是否仍然为正，置信区间是否大体不跨过0。

如果图中多个模型的系数均位于0右侧，并且置信区间不跨过0，说明主结论较稳定。该图是支撑主结论稳健性的核心图片之一。

### 4. fig4_final_model_coefficients.png / fig4_final_model_coefficients.pdf

图名含义：最终模型结果汇总系数图。

该图汇总更多模型结果，包括主模型、替代指标、机制相关结果和扩展模型结果。

它的优点是信息量大，能够一眼看到多数模型结果的方向和显著性。它的缺点是如果纳入模型过多，读者可能不容易分清哪些是主结论、哪些是机制检验、哪些只是边界检验。

该图适合用于内部结果总览，也可以放在论文中作为综合结果图。但若论文篇幅有限，建议将其拆分为“主结论与稳健性”“机制检验”“扩展模型”三张更清晰的图。

### 5. fig5_mechanism_coefficients.png / fig5_mechanism_coefficients.pdf

图名含义：机制路径系数图。

该图展示AI关注度或AI应用变量对若干机制变量的回归系数，例如实际AI应用指数、资产周转率、劳动生产率、TFP、质量子指数和效率子指数。

它回答的问题是：AI关注度为什么可能与提质增效相关。

如果AI关注度对实际AI应用、资产周转率、TFP等变量显著为正，可以支持这样的解释：AI关注度较高的企业可能通过促进实际AI应用、提高资产利用效率和改善综合要素效率，实现提质增效。

需要注意的是，该图展示的是机制相关性，不是严格中介因果链条。论文中应使用“可能通过”“与……相关”等表述。

### 6. fig6_heterogeneity_coefficients.png / fig6_heterogeneity_coefficients.pdf

图名含义：异质性分析系数图。

该图展示不同分组样本中的AI词频系数，例如国企与非国企、东部与非东部、高技术制造业与非高技术制造业等。

它回答的问题是：AI关注度与提质增效之间的关系在哪些企业中更明显。

如果国企、东部地区、高技术制造业企业的系数更大或显著性更强，可以说明AI赋能效果可能受到资源基础、数字基础设施和技术吸收能力影响。

该图适合放在异质性分析部分。更好的呈现方式是做成配对式森林图，让每组对比更清楚。

### 7. fig7_ai_application_quality_bins.png / fig7_ai_application_quality_bins.pdf

图名含义：AI应用水平分组与提质增效均值。

该图按照实际AI应用指数将样本分成五组，从低到高展示每组提质增效指数均值，并加入95%置信区间。

它回答的问题是：在不控制其他因素的原始数据中，AI应用水平更高的企业是否具有更高的提质增效均值。

当前图中，高AI应用组的提质增效均值明显高于低AI应用组，说明二者在描述性层面存在正向关系。该图直观、容易理解，适合作为主回归前的描述性证据。

需要特别注意的是，该图不是控制变量后的回归结果，也不能单独证明因果关系。它应与主回归表配合使用：图7提供直观关系，表1提供控制年份、行业和企业特征后的统计证据。

## 七、目前可视化还可以如何优化

### 1. 增加控制后偏相关图

这是最推荐新增的一张图。

当前 `fig7_ai_application_quality_bins` 展示的是原始分组关系，没有控制企业规模、资产负债率、研发强度、企业年龄、营业收入增长率、年份和行业差异。为了更直接体现X和Y在控制后的相关程度，可以新增控制后偏相关图。

具体做法是：

```text
第一步：用控制变量、年份固定效应和行业固定效应解释AI词频，取残差。
第二步：用控制变量、年份固定效应和行业固定效应解释提质增效指数，取残差。
第三步：画AI词频残差与提质增效残差的散点或分箱均值图，并加入拟合线。
```

这张图能够回答：在控制其他因素后，AI关注度较高的企业是否仍然具有更高的提质增效表现。

它比普通散点图更贴近主回归结果，也比单纯回归表更直观。

### 2. 增加分箱散点图

普通企业-年份散点可能非常密集，不容易看出关系。可以将AI词频按分位数分为20组或30组，计算每组AI均值和提质增效均值，再画分箱散点和拟合线。

这种图适合展示X和Y之间的总体相关方向，尤其适合样本量较大时使用。

需要注意的是，如果是原始分箱图，应标明“未控制其他因素”；如果是残差分箱图，应标明“控制年份、行业和企业特征后”。

### 3. 增加局部拟合曲线

可以使用LOESS或局部多项式拟合展示AI关注度与提质增效之间是否近似线性。

这类图能够服务非线性检验。如果曲线大体向上且没有明显弯曲，可以支持“主关系近似线性”；如果出现平台期或倒U型，则可以讨论边际效应变化。

当前非线性模型中二次项不显著，因此如果画局部拟合曲线，预期更适合支持“没有稳定非线性证据”的表述，而不是强调倒U型。

### 4. 增加分位数效应折线图

当前已有分位数固定效应结果，但没有专门的可视化。可以将25%、50%、75%分位点的AI系数画成折线图，并加入置信区间。

这张图能够直观展示：AI赋能效应是否在低提质增效企业中更强。

如果系数从25%分位到75%分位逐步下降，可以服务“低基础企业改善空间更大”的解释。

### 5. 优化机制图为路径图

当前机制图更像一组系数森林图，信息准确但路径感不强。可以将机制图改为路径结构：

```text
AI关注度
   ├── 实际AI应用
   ├── 资产周转率
   ├── TFP
   ├── 质量子指数
   └── 效率子指数
          ↓
      提质增效
```

每条路径旁边标注对应系数和显著性。这样更适合论文读者理解“AI为什么可能影响提质增效”。

### 6. 优化异质性图为配对森林图

当前异质性图可以进一步增强组间对比。建议按以下配对展示：

```text
国企 vs 非国企
东部 vs 非东部
高技术制造业 vs 非高技术制造业
高转化效率 vs 低转化效率
```

每一对放在相邻位置，使用不同颜色区分组别，并统一横轴尺度。这样读者更容易看出哪些组的AI系数更大、更显著。

### 7. 给fig7增加每组样本量

当前AI应用五分组图已经能展示正向关系，但建议在横轴标签中加入每组样本量，例如：

```text
低
n=5220
```

这样可以避免读者误以为某些组样本很少，也能说明五分组是按样本位置近似等分得到的。

### 8. 给所有系数图统一视觉规范

建议所有系数图统一使用：

- 横向森林图；
- 竖直零线；
- 点表示系数；
- 横线表示95%置信区间；
- 显著性用星号或浅色标注；
- 同类模型使用同一颜色；
- 图下注明标准误为公司聚类稳健标准误。

这样可以减少读者理解成本，使图表风格更像同一篇论文。

## 八、各类文件可用于哪些可视化

### 1. data/processed/manufacturing_modeling_panel.csv

这是最适合做“原始数据型可视化”的文件，因为它包含企业-年份层面的变量，而不是已经汇总好的回归结果。

可以用于绘制：

- AI词频与提质增效指数的原始散点图；
- AI词频与提质增效指数的分箱散点图；
- 控制后偏相关图；
- AI应用水平五分组均值图；
- 不同年份的AI关注度趋势图；
- 提质增效指数、质量子指数、效率子指数趋势图；
- 不同企业类型的箱线图或小提琴图；
- AI关注度和提质增效指数的相关系数热力图；
- 按行业或地区汇总的AI应用水平地图或柱状图。

如果要体现X和Y之间的相关程度，最推荐使用这个文件做“控制后偏相关图”和“分箱散点图”。

### 2. outputs/tables/final/paper_table_1_main_regression.csv

该表适合用于主结论可视化。

可以用于绘制：

- 主模型与替代X模型的系数森林图；
- 不同X口径下AI系数对比图；
- 显著性标记系数图；
- 主结果摘要卡片，例如系数、p值、样本量和拟合指标。

它最适合支撑“AI关注度与提质增效显著正相关”这一主结论。

### 3. outputs/tables/final/paper_table_2_robustness.csv

该表适合用于稳健性可视化。

可以用于绘制：

- 替换Y后的系数森林图；
- TFP、熵权指数、PCA指数三种Y口径的系数对比图；
- 研发缺失处理前后的结果对比图；
- 稳健性检验汇总图。

需要注意的是，研发缺失处理结果不显著，因此在图中应明确标注其为“边界检验”或“样本处理检验”，不要把它和主结果混在一起过度强调。

### 4. outputs/tables/final/paper_table_3_mechanism.csv

该表适合用于机制路径可视化。

可以用于绘制：

- 机制路径森林图；
- AI关注度到实际AI应用、资产周转率、TFP等机制变量的路径图；
- “AI关注度-经营机制-提质增效”的流程图；
- 显著机制和不显著机制的分组图。

如果想让机制部分更直观，建议把该表重画成路径图，而不是只做普通系数图。

### 5. outputs/tables/final/paper_table_4_heterogeneity.csv

该表适合用于异质性可视化。

可以用于绘制：

- 国企与非国企配对森林图；
- 东部与非东部配对森林图；
- 高技术制造业与非高技术制造业配对森林图；
- 不同企业类型AI系数排序图；
- 分组系数热力图。

它最适合支撑“AI赋能效果在哪些企业中更明显”的结论。可视化时最好按组别成对排列，读者会更容易看出差异。

### 6.outputs/tables/final/paper_table_5_conversion_moderation.csv

该表适合用于探索性调节效应可视化。

可以用于绘制：

- AI词频、高转化效率虚拟变量、交互项的系数图；
- 高低转化效率组的边际效应对比图；
- 调节效应示意图。

由于当前交互项不显著，这类图不建议放在论文核心位置。更适合放在探索性分析或附录中，用于说明“转化效率调节效应证据不足”。

### 7. outputs/tables/final/paper_table_6_advanced_models.csv

该表适合用于扩展模型可视化。

可以用于绘制：

- 分位数25%、50%、75%的AI系数折线图；
- 非线性一次项和二次项系数图；
- 固定效应主模型与交叉拟合模型的系数对比图；
- “模型扩展稳健性”综合图。

其中，最值得做的是分位数效应折线图，因为它可以直观显示AI作用是否随提质增效水平上升而减弱。

### 8. outputs/tables/final/paper_table_A1_boundary.csv

该表适合用于边界检验可视化。

可以用于绘制：

- 主模型与企业固定效应边界模型的系数对比图；
- 不同固定效应设定下AI系数变化图；
- 结论边界说明图。

这类图的作用不是强化主结论，而是说明结论边界。它适合放在附录或研究局限部分，帮助论文显得更诚实、更稳健。

### 9. outputs/tables/model_results/*.csv

这一组文件适合用于“精细化重画模型图”。相比 `tables/final`，这些文件包含更完整的模型名、公式、变量名、企业数、年份数和R²等信息。

可以用于绘制：

- 多模型系数森林图；
- 按模型类型分面的系数图；
- 样本量与系数大小的气泡图；
- 模型拟合指标对比图；
- 不同模型显著性矩阵图；
- 回归结果仪表板。

如果可视化人员要重新设计全部回归图，应该优先使用 `outputs/tables/model_results/`，而不是只用最终论文表。

### 10. outputs/tables/diagnostics/descriptive_statistics.csv

该表适合用于变量分布可视化。

可以用于绘制：

- 主要变量均值和标准差图；
- 箱线图；
- 变量范围和四分位数图；
- 描述性统计表的可视化版本。

它适合放在数据描述部分，用于说明变量的基本分布是否合理。

### 11. outputs/tables/diagnostics/missingness_summary.csv

该表适合用于缺失值可视化。

可以用于绘制：

- 各变量缺失率柱状图；
- 缺失率排序图；
- 缺失情况热力图。

它可以帮助解释为什么不同模型样本量不同，尤其是研发强度缺失为什么会影响主模型样本。

### 12. outputs/tables/diagnostics/quality_efficiency_index_components.csv

该表适合用于Y变量构造可视化。

可以用于绘制：

- 提质增效指数构成图；
- 质量维度和效率维度的结构图；
- 指标体系树状图；
- “提质”和“增效”双维度框架图。

这类图适合放在变量构造部分，让读者快速理解Y不是单一TFP，而是多维综合指标。

### 13. outputs/tables/diagnostics/variable_dictionary.csv

该表适合用于辅助可视化制作。

它本身不一定直接画图，但可以用于：

- 给图表轴标题改成中文可读名称；
- 给变量添加图例说明；
- 制作变量说明表；
- 统一论文中变量名称和图表名称。

如果交给外部人员做可视化，这个文件非常重要，因为它能避免把代码变量名直接放进图里。

### 14.outputs/tables/diagnostics/candidate_xy_screening_results.csv

该表适合用于变量筛选过程可视化。

可以用于绘制：

- 候选X-Y组合的系数热力图；
- 候选组合显著性矩阵；
- 不同Y指标下AI系数排序图；
- 变量选择过程说明图。

这类图不一定适合放进正文，但适合放在方法附录或答辩材料中，用于说明为什么最终选择当前X和Y。

### 15. outputs/tables/diagnostics/vif_diagnostics.csv

该表适合用于多重共线性诊断可视化。

可以用于绘制：

- VIF柱状图；
- 控制变量共线性风险排序图。

如果所有VIF都不高，可以用一张简单图说明模型不存在严重多重共线性问题。

### 16. outputs/tables/diagnostics/heteroskedasticity_tests.csv

该表适合用于异方差诊断可视化。

可以用于绘制：

- 异方差检验p值图；
- 不同模型诊断结果对比图。

它主要服务方法说明，帮助解释为什么回归中使用聚类稳健标准误。

### 17. outputs/tables/diagnostics/sample_integrity_checks.csv

该表适合用于样本筛选可视化。

可以用于绘制：

- 样本筛选流程图；
- 制造业样本完整性检查图；
- 数据清洗通过/未通过状态图。

它适合放在复现说明或附录中，用于证明非制造业企业没有错误进入建模样本。

### 18. outputs/logs/pipeline.log

日志文件一般不直接用于可视化，但可以支持可复现性说明。

如果需要，也可以从日志中提取运行步骤，制作：

- 数据处理流程图；
- 建模管线流程图；
- 输出文件生成流程图。

但更推荐直接根据代码流程手动画流程图，而不是从日志中机械提取。

## 九、不同论文位置推荐使用哪些可视化

### 1. 数据来源与变量构造部分

推荐使用：

- 提质增效指数构成图；
- 变量描述性统计图；
- 缺失率柱状图；
- 样本筛选流程图。

对应文件：

- `quality_efficiency_index_components.csv`
- `descriptive_statistics.csv`
- `missingness_summary.csv`
- `sample_integrity_checks.csv`

### 2. 描述性分析部分

推荐使用：

- AI关注度与AI应用趋势图；
- 提质增效指数趋势图；
- AI应用水平五分组均值图；
- 原始分箱散点图。

对应文件：

- `manufacturing_modeling_panel.csv`
- `fig1_ai_trend`
- `fig2_quality_efficiency_trend`
- `fig7_ai_application_quality_bins`

### 3. 基准回归部分

推荐使用：

- 主模型系数森林图；
- 控制后偏相关图；
- 主模型结果摘要图。

对应文件：

- `paper_table_1_main_regression.csv`
- `baseline_regression_results.csv`
- `final_fixed_effects_research_results.csv`
- `manufacturing_modeling_panel.csv`

### 4. 稳健性检验部分

推荐使用：

- 替换X系数对比图；
- 替换Y系数对比图；
- 研发缺失处理边界图；
- 多模型稳健性森林图。

对应文件：

- `paper_table_2_robustness.csv`
- `robustness_regression_results.csv`
- `final_fixed_effects_research_results.csv`

### 5. 机制检验部分

推荐使用：

- 机制路径图；
- 机制变量系数森林图；
- 显著机制与不显著机制对比图。

对应文件：

- `paper_table_3_mechanism.csv`
- `mechanism_regression_results.csv`

### 6. 异质性分析部分

推荐使用：

- 配对森林图；
- 分组系数排序图；
- 组间差异热力图。

对应文件：

- `paper_table_4_heterogeneity.csv`
- `heterogeneity_regression_results.csv`

### 7. 扩展模型与模型创新部分

推荐使用：

- 分位数效应折线图；
- 非线性局部拟合图；
- 交叉拟合与固定效应结果对比图。

对应文件：

- `paper_table_6_advanced_models.csv`
- `quantile_fe_results.csv`
- `nonlinear_ai_effect_results.csv`
- `cross_fitted_partial_linear_results.csv`

### 8. 研究局限与边界检验部分

推荐使用：

- 公司固定效应边界检验系数图；
- 不同固定效应设定下系数变化图；
- 主结论边界说明图。

对应文件：

- `paper_table_A1_boundary.csv`
- `firm_year_fe_boundary_tests.csv`

## 十、目前进度

当前结论最稳妥的表达是：AI关注度与制造业企业提质增效水平存在稳定正相关关系，该关系在多种变量替换和模型扩展下具有一定稳健性；但在更严格的企业固定效应边界检验下会变弱，因此不应过度表述为严格因果效应。
